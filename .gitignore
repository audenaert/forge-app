# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
out/

# GraphQL codegen output — typed documents are committed so the build
# does not depend on a running API server. Re-run `npm run codegen
# --workspace=apps/web` when operations or the schema change.
!apps/web/src/lib/graphql/generated/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Claude worktrees (local dev only)
.claude/worktrees/

# Editor
.vscode/
*.swp
*.swo
.claude/settings.local.json
