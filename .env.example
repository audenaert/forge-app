# Neo4j connection
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=forgedev

# API server
PORT=4000
# Set to "true" to skip API key auth (local development only — never in production)
# DISABLE_AUTH=true
