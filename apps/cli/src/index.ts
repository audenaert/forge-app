// Library entry point for @etak/cli.
//
// The package is consumed primarily as a bin (`etak`), but this module exists
// so tsup/tsc have a stable library surface as the internals grow. It is
// intentionally empty in this scaffold — schemas, adapters, and formatters
// land in later stories.
export {};
