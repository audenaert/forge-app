import { describe, it, expect } from 'vitest';

// Placeholder so the `unit` vitest project has at least one file to discover.
// Real unit tests for schemas, formatters, and slug logic land in later
// stories (M1-S3 onward).
describe('unit project placeholder', () => {
  it('is wired up', () => {
    expect(true).toBe(true);
  });
});
