import { describe, it, expect } from 'vitest';

// Placeholder so the `adapter-contract` vitest project has at least one file
// to discover. The real shared adapter contract suite lands in M1-S4.
describe('adapter-contract project placeholder', () => {
  it('is wired up', () => {
    expect(true).toBe(true);
  });
});
